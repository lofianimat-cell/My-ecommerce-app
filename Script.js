// Example: popup when Buy Now is clicked
document.querySelectorAll("button").forEach(btn => {
  btn.addEventListener("click", () => {
    alert("Thanks for shopping with Al-Noor Collection!");
  });
});
